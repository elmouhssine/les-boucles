#include <stdio.h>
#include <stdlib.h>

int main()
{
  int a,b,s;
   printf("nombre  a :");
   scanf("%d",&a);
     printf("nombre b :");
   scanf("%d",&b);

   if(a==b){
    printf("%d",s=(a+b)*3);
   }
   else{
        printf("%d",s=(a+b));
   }
}
